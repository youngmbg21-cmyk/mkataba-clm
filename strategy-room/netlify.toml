[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Single-page app: every route falls back to index.html,
# except the function endpoints.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
